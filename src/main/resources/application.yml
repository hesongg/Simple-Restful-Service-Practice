server:
  port: 8088

logging:
  level:
    org.springframework: debug

spring:
  messages:
    basename: messages
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  security:
    user:
      name: jhs
      password: testpass

management:
  endpoints:
    web:
      exposure:
        include: "*"